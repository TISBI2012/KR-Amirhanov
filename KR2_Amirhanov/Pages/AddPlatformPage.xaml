<Page x:Class="KR2_Amirhanov.Pages.AddPlatformPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KR2_Amirhanov.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddPlatformPage"
      DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <TextBlock Text="Название платформы:" Margin="5"/>
            <TextBox Margin="5" Text="{Binding Platform.Name, Mode=TwoWay}"/>
            <TextBlock Text="Отдел:" Margin="5"/>
            <ComboBox x:Name="DepartmentBox" SelectedValue="{Binding Platform.DepartmentID, Mode=TwoWay}" Margin="5"/>

            <Button Content="Добавить платформу" Margin="5" Click="AddPlatform_Click"/>
        </StackPanel>
    </Grid>
</Page>
